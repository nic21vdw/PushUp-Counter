See [AGENTS.md](AGENTS.md). It is the single source of truth for how to work on
this project, and the short version is: commit straight to `main`, run
`npm test`, restart the server, hard-reload the page.
